{
  "name": "@jellyfin-tv/server-shell-drop",
  "version": "1.0.0",
  "private": true,
  "description": "Canonical layout + manifest emitter for ${server}/shell/ — the server-hosted drop folder that the Hosted Shell Bootstrap pulls shell.min.js from at launch.",
  "license": "GPL-2.0-only",
  "scripts": {
    "emit-manifest": "python3 scripts/emit_manifest.py"
  }
}
