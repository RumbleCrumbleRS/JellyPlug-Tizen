{
  "name": "@jellyfin-tv/shell-core",
  "version": "1.0.0",
  "private": true,
  "description": "Single-source-of-truth raw JS fragment for functions mirrored across the retail shell (shell.js) and the HSB boot shell (boot-shell.src.js). Spliced into both at build time via //@@SHELL_CORE:name@@ markers + a shared expand() step (JEL-644).",
  "license": "GPL-2.0-only",
  "scripts": {
    "test": "node scripts/expand.test.cjs"
  }
}
