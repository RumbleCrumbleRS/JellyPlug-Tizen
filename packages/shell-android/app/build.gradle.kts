// Android Leanback host activity build script.
// Stubbed; lands in M5. Documents the intended target so the scaffold is
// self-explanatory.

plugins {
    // id("com.android.application")
    // id("org.jetbrains.kotlin.android")
}

android {
    // namespace = "org.jellyfin.tv"
    // compileSdk = 34
    // defaultConfig {
    //     applicationId = "org.jellyfin.tv"
    //     minSdk = 24      // Android 7.0, covers most Android TV devices
    //     targetSdk = 34
    //     versionCode = 1
    //     versionName = "0.0.0"
    // }
    // // Mixed-content allowance: most self-hosted Jellyfin servers run on HTTP.
    // // Android requires usesCleartextTraffic=true plus a domain config.
}
